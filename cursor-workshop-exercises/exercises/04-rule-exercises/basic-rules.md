# 基礎規則創建練習

## 🎯 練習目標
學習創建基本的專案規則，讓 AI 更好地理解專案需求

## 📋 練習步驟

### 練習 1: 創建基本專案規則
**任務**: 為待辦事項應用創建基本規則

**操作步驟**:
1. 在專案根目錄創建 `.cursor/rules` 文件
2. 添加以下基本規則：

```markdown
# 待辦事項應用開發規則

## 專案概述
這是一個簡單的待辦事項應用，使用 HTML、CSS 和 JavaScript 開發。

## 技術棧
- HTML5
- CSS3
- Vanilla JavaScript (不使用框架)
- 本地存儲 (localStorage)

## 基本要求
- 所有代碼必須有適當的註解
- 使用有意義的變數和函數名稱
- 遵循一致的代碼格式
- 添加錯誤處理
```

### 練習 2: 添加程式碼風格規則
**任務**: 設定程式碼風格和格式規則

**操作步驟**:
1. 在 `.cursor/rules` 文件中添加：

```markdown
## 程式碼風格
- 使用 2 個空格縮排
- 使用單引號而不是雙引號
- 在函數和變數之間添加空行
- 使用 camelCase 命名變數和函數
- 使用 PascalCase 命名類別
- 常數使用 UPPER_SNAKE_CASE

## 函數規範
- 每個函數都應該有 JSDoc 註解
- 函數長度不超過 50 行
- 參數不超過 5 個
- 優先使用箭頭函數
```

### 練習 3: 添加功能規則
**任務**: 設定功能開發規則

**操作步驟**:
1. 在 `.cursor/rules` 文件中添加：

```markdown
## 功能開發規則
- 每個功能都應該有對應的測試
- 優先使用純函數
- 避免全局變數
- 使用模組化設計
- 添加輸入驗證
- 提供用戶友好的錯誤信息

## 待辦事項功能規則
- 任務必須有標題和狀態
- 支持任務的增刪改查
- 使用 localStorage 持久化數據
- 提供任務篩選功能
- 支持任務優先級
```

### 練習 4: 測試規則效果
**任務**: 測試規則是否生效

**操作步驟**:
1. 使用 Chat 模式，輸入：
```
請為待辦事項應用創建一個新的功能：任務分類
```

2. 觀察 AI 是否遵循了設定的規則：
- 是否使用了正確的命名規範
- 是否添加了適當的註解
- 是否遵循了程式碼風格
- 是否添加了錯誤處理

### 練習 5: 規則優化
**任務**: 根據測試結果優化規則

**操作步驟**:
1. 如果 AI 沒有完全遵循規則，更新 `.cursor/rules` 文件
2. 添加更具體的規則：

```markdown
## 具體實現規則
- 所有 DOM 操作都必須檢查元素是否存在
- 使用 try-catch 包裝所有 localStorage 操作
- 每個事件監聽器都必須有對應的移除邏輯
- 使用 data 屬性而不是 class 來標識元素
- 所有用戶輸入都必須進行驗證和清理
```

### 練習 6: 個人風格規則
**任務**: 添加個人編程風格偏好

**操作步驟**:
1. 在 `.cursor/rules` 文件中添加：

```markdown
## 個人風格偏好
- 優先使用 const，其次 let，避免 var
- 使用模板字串而不是字串拼接
- 使用解構賦值來提取物件屬性
- 使用展開運算符來複製陣列和物件
- 優先使用陣列方法 (map, filter, reduce) 而不是迴圈
- 使用 async/await 而不是 Promise.then()
```

## 🔍 觀察要點

### 規則生效的表現
1. **命名規範**: AI 生成的變數和函數名稱符合規則
2. **程式碼風格**: 縮排、引號、空行等符合規則
3. **功能實現**: 按照規則實現功能
4. **錯誤處理**: 添加了適當的錯誤處理
5. **註解**: 添加了適當的註解和文檔

### 規則優化技巧
1. **具體化**: 規則越具體，AI 越容易遵循
2. **範例化**: 提供具體的範例
3. **分層**: 將規則分為不同層級
4. **測試**: 定期測試規則是否生效
5. **迭代**: 根據使用情況不斷優化規則

## 💡 進階練習

### 練習 7: 複雜專案規則
為一個完整的電商網站創建規則，包含：
- 前端規則
- 後端規則
- 資料庫規則
- 安全規則
- 性能規則

### 練習 8: 團隊協作規則
創建適合團隊協作的規則，包含：
- 代碼審查規則
- 提交訊息規則
- 分支管理規則
- 文檔規則

## 📝 反思問題
1. 規則如何影響 AI 的代碼生成？
2. 什麼樣的規則最有效？
3. 如何平衡規則的詳細程度和靈活性？
4. 規則是否需要定期更新？