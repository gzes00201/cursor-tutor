// TAB 練習 3: 類別和物件
// 目標: 練習 Tab 在物件導向編程中的應用

// 練習 3.1: 基本類別定義
// 輸入 "class Person" 然後按 Tab
class Person

// 練習 3.2: 類別建構函數
// 輸入 "constructor" 然後按 Tab
constructor

// 練習 3.3: 類別方法
// 輸入 "getName" 然後按 Tab
getName

// 練習 3.4: 靜態方法
// 輸入 "static create" 然後按 Tab
static create

// 練習 3.5: 繼承
// 輸入 "class Student extends Person" 然後按 Tab
class Student extends Person

// 練習 3.6: 物件字面量
// 輸入 "const person = {" 然後按 Tab
const person = {

// 練習 3.7: 物件方法
// 輸入 "const calculator = {" 然後按 Tab
const calculator = {

// 練習 3.8: 解構賦值
// 輸入 "const { " 然後按 Tab
const { 

// 練習 3.9: 展開運算符
// 輸入 "const merged = { ..." 然後按 Tab
const merged = { ...

// 練習 3.10: 物件屬性存取
// 輸入 "person." 然後按 Tab
person.

// 進階練習: 觀察 Tab 如何理解物件結構
const user = {
    name: "John",
    age: 30,
    email: "john@example.com"
};

// 輸入 "user." 然後按 Tab，觀察可用的屬性
user.

// 輸入 "const { name, " 然後按 Tab
const { name, 

// 輸入 "const newUser = { ...user, " 然後按 Tab
const newUser = { ...user, 

// 練習: 創建一個完整的類別
// 輸入註解，然後定義類別，觀察 Tab 如何根據註解生成類別結構
// 這是一個汽車類別，包含品牌、型號、年份等屬性
class Car

// 這是一個銀行帳戶類別，包含餘額、存款、提款等方法
class BankAccount

// 這是一個購物車類別，包含商品列表、添加商品、計算總價等方法
class ShoppingCart