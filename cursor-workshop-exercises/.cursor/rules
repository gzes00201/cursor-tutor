# Cursor Workshop 練習專案規則

## 專案概述
這是一個用於 Cursor 教學課程的練習專案，包含多個練習題目和範例專案。

## 技術棧
- HTML5
- CSS
- Vanilla JavaScript
- 本地儲存 (localStorage)

## 程式碼風格
- 使用 2 個空格縮排
- 使用單引號而不是雙引號
- 在函數和變數之間添加空行
- 使用 camelCase 命名變數和函數
- 使用 PascalCase 命名類別
- 常數使用 UPPER_SNAKE_CASE

## 函數規範
- 每個函數都應該有 JSDoc 註解
- 函數長度不超過 50 行
- 參數不超過 5 個
- 優先使用箭頭函數
- 使用 const 和 let，避免 var

## 功能開發規則
- 每個功能都應該有對應的測試
- 優先使用純函數
- 避免全域變數
- 使用模組化設計
- 添加輸入驗證
- 提供使用者友善的錯誤訊息

## 待辦事項應用規則
- 任務必須有標題和狀態
- 支援任務的增刪改查
- 使用 localStorage 持久化資料
- 提供任務篩選功能
- 支援任務優先級和分類

## 具體實作規則
- 所有 DOM 操作都必須檢查元素是否存在
- 使用 try-catch 包裝所有 localStorage 操作
- 每個事件監聽器都必須有對應的移除邏輯
- 使用 data 屬性而不是 class 來標識元素
- 所有使用者輸入都必須進行驗證和清理

## 個人風格偏好
- 優先使用 const，其次 let，避免 var
- 使用模板字串而不是字串拼接
- 使用解構賦值來提取物件屬性
- 使用展開運算子來複製陣列和物件
- 優先使用陣列方法 (map, filter, reduce) 而不是迴圈
- 使用 async/await 而不是 Promise.then()

## 錯誤處理
- 所有非同步操作都必須有錯誤處理
- 使用 try-catch 包裝可能出錯的操作
- 提供有意義的錯誤訊息
- 記錄錯誤到控制台

## 效能優化
- 避免不必要的 DOM 操作
- 使用事件委託
- 適當使用快取
- 避免記憶體洩漏

## 安全規則
- 所有使用者輸入都必須進行驗證和清理
- 避免使用 eval() 和 innerHTML
- 使用 textContent 而不是 innerHTML
- 驗證所有外部資料

## 文件規則
- 每個檔案都應該有檔案頭註解
- 複雜的邏輯必須有註解
- 使用 JSDoc 格式的函數註解
- 提供使用範例

## 測試規則
- 每個功能都應該有對應的測試
- 測試應該涵蓋正常情況和邊界情況
- 使用描述性的測試名稱
- 測試應該獨立且可重複

## 版本控制規則
- 使用有意義的提交訊息
- 每次提交應該只包含一個邏輯變更
- 使用分支進行功能開發
- 定期合併主分支

## 部署規則
- 確保所有功能在生產環境中正常工作
- 使用環境變數管理設定
- 提供適當的錯誤處理
- 監控應用效能